# School CRUD React App

Simple React app (Vite) to view/add/edit/delete students via provided API endpoints.

Run locally:

```bash
cd "d:/java_sts/SpringProjects/azure deploy/frontend"
npm install
npm run dev
```

App will open at `http://localhost:5173` by default.
